subprojects{

    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

}

// to load gradle version run: ./gradlew wrapper
task wrapper(type: Wrapper) {
    gradleVersion: '4.10.3'
}